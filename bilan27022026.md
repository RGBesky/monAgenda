# Bilan Complet — monAgenda V2 · 27 Février 2026

> **Audit technique ligne par ligne + Analyse de faisabilité Phase 3**
> Réalisé par Opus 4.6 sur la base du code source commit `a5d8a13`

---

# PARTIE 1 : BILAN DE LA JOURNÉE

## Travaux réalisés le 27/02/2026

| # | Travail | Fichiers impactés | Statut |
|---|---|---|---|
| 1 | **SnackBars persistants** — 46 appels `ScaffoldMessenger.of(context)` → clé globale `scaffoldMessengerKey` | `app.dart` + 10 écrans features | ✅ OK |
| 2 | **Dark mode — contraste** — 8 corrections (icônes, texte SnackBar, tags, error banner, chips) | `event_form_screen.dart`, `calendar_screen.dart`, `agenda_screen.dart`, `event_detail_screen.dart`, `app.dart` | ✅ OK |
| 3 | **Logo sidebar** — Toujours le logo couleur (plus de logo blanc invisible en mode clair) | `app.dart` | ✅ OK |
| 4 | **Logo Notion** — Supprimé `color: Colors.white + BlendMode.srcIn`. Ajout `ClipRRect` coins arrondis. | `source_logos.dart`, `settings_screen.dart` | ✅ OK |
| 5 | **Notion description multi-propriétés** — `descriptionProperties` = `List<String>` (JSON en SQLite). UI chips multi-select. | `notion_database_model.dart`, `connections_settings_screen.dart`, `notion_service.dart` | ✅ OK |
| 6 | **kDrive backup via lien de dépôt** — Réécriture complète `BackupService` (AES-256, sans OAuth2) | `backup_service.dart`, `backup_settings_screen.dart`, `settings_provider.dart`, `app_constants.dart` | ⚠️ Local OK / Upload KO |
| 7 | **Fix workmanager** — Mis à jour 0.5.2 → 0.6.0 + résolution conflit `work-runtime-ktx` | `pubspec.yaml`, `android/app/build.gradle` | ✅ OK |
| 8 | **Version 2.0.0** — Bump dans pubspec.yaml | `pubspec.yaml` | ✅ OK |
| 9 | **Build APK** — `MonAgenda_v2.0.0_release.apk` (95 Mo) | `/home/besky/Documents/Testeur/` | ✅ OK |
| 10 | **Build .deb** — `monagenda_1.0.0-3_amd64.deb` (18 Mo) Linux | `build/` | ✅ OK |

**Commit** : `a5d8a13` — 107 fichiers, +6 388 / -1 069 lignes.

---

## Audit vs Roadmap — Écart

| Audit (prévu) | Réalisé | Écart |
|---|---|---|
| **Phase 1.1** — Sécuriser l'export AES-256 | Fait (package `encrypt`, chiffrement AES-256 dans `BackupService`) | Aucun |
| **Phase 1.2** — Scanner QR import mobile | Fait (`mobile_scanner`, `ImportConfigScreen`) | Aucun |
| **Phase 1.3** — Notifications WorkManager | `workmanager` 0.6.0 + tâches périodiques 6h + reschedule immédiat | À valider sur device Android |
| **Phase 1.4** — Hardcoding locale fr_FR | Fait (`main.dart` : `Intl.defaultLocale = 'fr_FR'`, `Europe/Paris`) | Aucun |
| **Phase 2.1** — Sync Queue | **Fait** — audit complet ci-dessous | Aucun |
| **Phase 2.2** — Gestion erreurs / system_logs | **Fait** — audit complet ci-dessous | Aucun |
| **Phase 2.3** — Validation schéma Notion | **Fait** — audit complet ci-dessous | Aucun |
| Hors audit — Dark mode, SnackBars, logos | Fait intégralement | Bonus UX terrain |
| Hors audit — kDrive deposit link | Local OK, remote KO (endpoint Infomaniak non trouvé) | Bloqué API externe |
| Hors audit — Notion multi-description | Fait | Bonus fonctionnel |

---

# PARTIE 2 : AUDIT COMPLET PHASE 2 (OFFLINE-FIRST)

> `flutter analyze` : **0 erreur, 0 warning, 12 infos** (cosmétiques uniquement)

---

## 2.1 — Sync Queue

### Base de données

| Élément | Fichier | Ligne(s) | Statut |
|---|---|---|---|
| Table `sync_queue` (id, action, source, event_id, payload, created_at, retry_count, last_error, status) | `lib/core/database/database_helper.dart` | L137-150 (onCreate) | ✅ |
| Index `idx_sync_queue_status` | `lib/core/database/database_helper.dart` | L151 | ✅ |
| Migration versionnée `oldVersion < 6` | `lib/core/database/database_helper.dart` | L181-199 | ✅ |
| Constante `tableSyncQueue = 'sync_queue'` | `lib/core/constants/app_constants.dart` | L37 | ✅ |
| `dbVersion = 6` | `lib/core/constants/app_constants.dart` | L27 | ✅ |

### Méthodes DB (7 méthodes)

| Méthode | Description | Ligne | Statut |
|---|---|---|---|
| `enqueueSyncAction()` | Insère action+source+payload avec `status=pending` | L685 | ✅ |
| `getPendingSyncActions()` | `WHERE status='pending' ORDER BY created_at ASC` | L703 | ✅ |
| `completeSyncAction(id)` | DELETE (supprime l'action réussie) | L711 | ✅ |
| `failSyncAction(id, error)` | `retry_count++`, passe en `failed` si ≥ 5 retries | L719 | ✅ |
| `getPendingSyncCount()` | COUNT pending actions | L730 | ✅ |
| `clearFailedSyncActions()` | DELETE `status='failed'` | L740 | ✅ |
| `completePendingSyncActionsForEvent(eventId)` | Supprime doublons queue après push direct réussi | L749 | ✅ |

### SyncQueueWorker (210 lignes)

| Composant | Fichier | Détail | Statut |
|---|---|---|---|
| `SyncAction` (create/update/delete) | `lib/services/sync_queue_worker.dart` L12-16 | Constantes d'action | ✅ |
| `processQueue()` | L42-68 | Itère les pending, traite, complete ou fail | ✅ |
| `_processInfomaniakAction()` | L90-115 | PUT (create/update) + DELETE | ✅ |
| `_processNotionAction()` | L117-189 | Create (avec update pageId local) + Update + Archive | ✅ |
| Guard `_isProcessing` | L43 | Anti-concurrence | ✅ |
| Logging AppLogger | Partout | Chaque action loguée info/error | ✅ |

### Optimistic UI (EventsNotifier)

| Méthode | Fichier | Comportement | Statut |
|---|---|---|---|
| `createEvent()` | `lib/providers/events_provider.dart` L103 | `insertEvent()` → `invalidateSelf()` → `enqueueSyncAction(create)` | ✅ |
| `updateEvent()` | L122 | `updateEvent()` → `invalidateSelf()` → `enqueueSyncAction(update)` | ✅ |
| `deleteEvent()` | L136 | Récupère event → `deleteEvent()` → `invalidateSelf()` → `enqueueSyncAction(delete)` | ✅ |
| Enqueue seulement si Infomaniak/Notion | L109, L128, L145 | `if (event.source == sourceInfomaniak \|\| sourceNotion)` | ✅ |
| `BackgroundWorkerService.rescheduleNow()` | L118, L135, L159 | Reprogramme notifs après chaque CRUD | ✅ |

### Providers de synchronisation

| Provider | Fichier | Rôle | Statut |
|---|---|---|---|
| `syncQueueWorkerProvider` | `lib/providers/sync_provider.dart` L81 | Crée le worker avec ref.watch sur services | ✅ |
| `pendingSyncCountProvider` | L88 | FutureProvider → `getPendingSyncCount()` | ✅ |
| `forceOfflineProvider` | L282 | StateNotifier + SharedPreferences persisté | ✅ |
| `isOfflineProvider` | L313 | Combine forceOffline + connectivity_plus | ✅ |
| `autoSyncOnConnectivityProvider` | L319 | Watch connectivity → syncAll() avec debounce 5s | ✅ |
| `periodicSyncRetryProvider` | L349 | Timer 60s (fallback Linux) → sync si pending > 0 | ✅ |

### Flux syncAll() et pushAndSync()

| Étape | Fichier:Ligne | Détail | Statut |
|---|---|---|---|
| `syncAll()` dépile la queue d'abord | `sync_provider.dart` L150 | `queueWorker.processQueue()` avant `engine.syncAll()` | ✅ |
| Bloquer si mode offline forcé | L140 | `if (forceOffline) return offline` | ✅ |
| `pushAndSync()` | L217 | Désactive offline → drain queue → sync complète → invalidate providers | ✅ |

### Nettoyage queue post push direct

| Lieu | Fichier:Ligne | Statut |
|---|---|---|
| Après `pushEventToInfomaniak` | `lib/services/sync_engine.dart` L458 | ✅ |
| Après `pushEventToNotion` | `lib/services/sync_engine.dart` L505 | ✅ |

### UI Offline

| Composant | Fichier:Ligne | Détail | Statut |
|---|---|---|---|
| Bannière offline + compteur pending | `lib/app.dart` L730-784 | Visible quand `isOffline`, affiche `"$count en attente"` | ✅ |
| Mini-FAB 3 états (vert/orange/rouge) | `lib/app.dart` L615-650 | Toggle forceOffline, toujours visible dans NavigationRail | ✅ |
| Providers watchés dans AppShell | `lib/app.dart` L393-395 | `autoSyncOnConnectivity` + `periodicSyncRetry` | ✅ |

**Verdict Phase 2.1 : ✅ COMPLET**

---

## 2.2 — Gestion des Erreurs Silencieuses

### Base de données

| Élément | Fichier:Ligne | Statut |
|---|---|---|
| Table `system_logs` (id, level, source, message, details, created_at, is_read) | `database_helper.dart` L155-167 | ✅ |
| Index `idx_system_logs_level` | L168 | ✅ |
| Migration v6 `CREATE TABLE IF NOT EXISTS` | L196-210 | ✅ |
| Constante `tableSystemLogs = 'system_logs'` | `app_constants.dart` L38 | ✅ |

### Méthodes DB (6 méthodes)

| Méthode | Description | Ligne | Statut |
|---|---|---|---|
| `insertSystemLog()` | Insère level+source+message+details+created_at | L759 | ✅ |
| `getUnreadLogs()` | `WHERE is_read=0 LIMIT 50` | L775 | ✅ |
| `getRecentLogs()` | Dernières 24h | L784 | ✅ |
| `markAllLogsAsRead()` | `UPDATE is_read=1` | L795 | ✅ |
| `getUnreadErrorCount()` | `WHERE is_read=0 AND level='error'` | L803 | ✅ |
| `cleanOldLogs()` | DELETE > 7 jours | L813 | ✅ |

### AppLogger centralisé

| Composant | Fichier | Détail | Statut |
|---|---|---|---|
| Singleton `AppLogger.instance` | `lib/services/logger_service.dart` | 55 lignes | ✅ |
| `debug()` | — | assert-only (dev), pas persisté | ✅ |
| `info()` | — | Persisté en BDD via `insertSystemLog` | ✅ |
| `warning()` | — | Persisté en BDD | ✅ |
| `error()` | — | Persisté en BDD | ✅ |
| Pas de `print()` en prod | — | Règle d'or respectée | ✅ |

### Logger utilisé dans

| Service/Provider | Utilisation |
|---|---|
| `sync_queue_worker.dart` | Chaque action : info succès, error échec |
| `sync_engine.dart` | Push Infomaniak/Notion, delete, schéma invalide |
| `sync_provider.dart` | AutoSync, PeriodicRetry, pushAndSync |
| `events_provider.dart` | Enqueue failures |
| `settings_provider.dart` (providers) | Config services info/warning |

### UI indicateur d'erreurs

| Composant | Fichier:Ligne | Détail | Statut |
|---|---|---|---|
| Provider `unreadErrorCountProvider` | `sync_provider.dart` L92 | FutureProvider → `getUnreadErrorCount()` | ✅ |
| Badge rouge dans Paramètres | `settings_screen.dart` L402-417 | Icône ⚠️ rouge + badge compteur (cap 9+) | ✅ |
| Import `system_logs_screen.dart` | `settings_screen.dart` L25 | Présent | ✅ |
| Navigation vers SystemLogsScreen | `settings_screen.dart` L437 | `onTap` → `MaterialPageRoute` | ✅ |
| **SystemLogsScreen** | `system_logs_screen.dart` | 193 lignes. Liste logs 24h, ExpansionTile détails, bouton nettoyer, auto mark-as-read | ✅ |

**Verdict Phase 2.2 : ✅ COMPLET**

---

## 2.3 — Validation du Schéma Notion

### NotionSchemaValidator

| Composant | Fichier:Ligne | Détail | Statut |
|---|---|---|---|
| `SchemaValidationResult` model | `notion_schema_validator.dart` L4-27 | `databaseName`, `databaseId`, `missingProperties`, `warnings`, `isValid` | ✅ |
| `validateAll()` | L47-73 | Itère les BDD enabled. Logue warnings AppLogger. | ✅ |
| `validateDatabase()` | L88-155 | Vérifie existence + type de Date, Category/Tags, Status, Titre | ✅ |
| Vérification type Date | L105-115 | Obligatoire. Doit être `date`. | ✅ |
| Vérification type Category | L118-130 | Recommandé. `select` ou `multi_select`. | ✅ |
| Vérification type Status | L133-145 | Recommandé. `status` ou `select`. | ✅ |
| Vérification type Titre | L148-155 | Doit être `title`. | ✅ |
| `_propType()` helper | L158-161 | Extraction type depuis schéma Notion | ✅ |

### Intégration au démarrage

| Lieu | Fichier:Ligne | Détail | Statut |
|---|---|---|---|
| Appelé dans `syncAll()` avant `_syncNotion()` | `sync_engine.dart` L82-95 | `validator.validateAll(notionDbs)` | ✅ |
| Résultats invalides → `errors` map | L87-91 | `errors['Notion:{dbName}'] = 'Schéma invalide — ...'` | ✅ |
| Logging via AppLogger | L92-95 | Warning si bases invalides | ✅ |

**Verdict Phase 2.3 : ✅ COMPLET**

---

## Résumé quantitatif Phase 2

| Composant | Lignes de code | Fichier |
|---|---|---|
| Table `sync_queue` + 7 méthodes DB | ~90 | `database_helper.dart` |
| Table `system_logs` + 6 méthodes DB | ~70 | `database_helper.dart` |
| Migration v6 | ~30 | `database_helper.dart` |
| `SyncQueueWorker` | 210 | `sync_queue_worker.dart` |
| `NotionSchemaValidator` | 167 | `notion_schema_validator.dart` |
| `AppLogger` | 55 | `logger_service.dart` |
| `SystemLogsScreen` | 193 | `system_logs_screen.dart` |
| `EventsNotifier` (optimistic UI) | ~80 ajoutées | `events_provider.dart` |
| `SyncNotifier` (queue + auto-retry) | ~100 ajoutées | `sync_provider.dart` |
| Providers (5 nouveaux) | ~50 | `sync_provider.dart` |
| UI offline (banner + mini-FAB) | ~100 | `app.dart` |
| **Total Phase 2** | **~1 145 lignes** | |

---

## Observations mineures (polish, non-bloquantes)

| # | Observation | Impact | Priorité |
|---|---|---|---|
| 1 | `deleteEvent` récupère l'event depuis la plage affichée — si l'event est hors vue, la suppression remote ne sera pas enqueued | Edge case rare | Basse |
| 2 | Pas de déduplication dans la queue — éditions rapides empilent des `update` redondants | Fonctionnel mais gaspillage | Basse |
| 3 | `pendingSyncCountProvider` pas invalidé après create/update dans `EventsNotifier` | Le compteur se met à jour au prochain cycle sync, pas immédiatement | Basse |
| 4 | Le compteur pending n'est visible que quand offline | Si online mais queue pas drainée, l'user ne voit pas le count | Basse |
| 5 | `SyncQueueWorker` ne gère pas la déduplication — si 3 updates du même event sont en queue, il exécute les 3 | Dernier seul pertinent. Pas de régression. | Basse |
| 6 | Pas d'avertissement dans `ConnectionsSettingsScreen` si actions pending avant changement de credentials | Risque de sync failures après changement de mot de passe | Moyenne |

---

# PARTIE 3 : ANALYSE DE FAISABILITÉ — PHASE 3 (IA LOCALE)

> Évaluation selon les 3 critères prioritaires : **Sécurité/Souveraineté**, **Poids**, **Fluidité**

## Rappel du scope Phase 3

- **Danube 3** (H2O.ai, quantifié 4-bit GGUF) pour parsing texte naturel → `EventModel`
- Exécution locale via **MediaPipe / TFLite**
- Zéro appel réseau
- Téléchargement on-demand (~500 Mo)

---

## A. Sécurité & Souveraineté (priorité haute)

| Critère | Évaluation | Détail |
|---|---|---|
| **Données restent en local** | ⭐ Excellent | Zero Trust par design. Le modèle tourne on-device, aucune requête API pour le NLP. C'est le point fort absolu de cette approche. |
| **Modèle open-source** | ⭐ Bon | Danube 3 est Apache 2.0, poids vérifiables, pas de telemetry. Pas de licence commerciale piégeuse. |
| **Téléchargement du modèle** | ⚠️ Attention | Le modèle doit être téléchargé depuis une source sûre. Si kDrive public, il faut vérifier l'intégrité (hash SHA-256). **Risque : man-in-the-middle si pas de vérification de checksum.** |
| **Recommandation** | — | Stocker le hash SHA-256 dans l'APK. Après téléchargement, comparer le hash. Rejeter si mismatch. |
| **Stockage local du modèle** | ⭐ OK | `ApplicationDocumentsDirectory` est sandboxé sur Android (inaccessible aux autres apps). |

**Score sécurité/souveraineté : 9/10**

---

## B. Poids de l'application

### État actuel

| Élément | Poids |
|---|---|
| APK release (arm64 + arm32) | **95 Mo** |
| .deb Linux (x64) | **18 Mo** |

### Projection avec Phase 3

| Élément | Poids actuel | Avec Phase 3 | Impact |
|---|---|---|---|
| **APK** | 95 Mo | ~110 Mo | +15 Mo (librairies TFLite natives `.so`) |
| **Modèle Danube 3 (4-bit)** | — | ~500 Mo (téléchargé on-demand) | Stocké dans AppDocuments, hors APK |
| **Total sur device** | 95 Mo | **~610 Mo** | |

### Optimisations recommandées *avant* Phase 3

| Action | Gain estimé | Effort |
|---|---|---|
| Activer `minifyEnabled: true` + `shrinkResources: true` (build.gradle) | APK 95 → ~55 Mo | 1 ligne |
| Build `--split-per-abi` (arm64 uniquement) | APK ~55 → ~35 Mo | 1 flag |
| Vérifier `google_fonts` — potentiellement retirer le bundle offline | Variable | À auditer |

**Score poids : 6/10** — Faisable. 610 Mo total sur device acceptable pour usage personnel. APK actuel trop gros (optimisable).

---

## C. Fluidité / Performance

| Aspect | Évaluation | Détail |
|---|---|---|
| **Temps chargement modèle** | 2-5 secondes | Cold load en mémoire au premier appel. Ensuite instantané tant que l'app vit. Acceptable si lazy load (pas au démarrage). |
| **Temps d'inférence** | 0.5-2 secondes | Pour une phrase courte sur Snapdragon 6xx+. Fluide pour saisie "tapée puis validée". Pas assez rapide pour du keystroke-by-keystroke temps réel. |
| **RAM** | ~800 Mo - 1.2 Go | Modèle 4-bit 500 Mo en mémoire. **Risque OOM sur téléphones 4 Go de RAM.** Le HP Laptop Linux est OK. |
| **CPU/GPU** | TFLite délégués GPU/NNAPI | Performant sur flagship/mid-range. Lent sur entrée de gamme. |
| **Impact UI** | Doit tourner dans un **Isolate** | Si mal implémenté, freeze toute l'UI pendant l'inférence. |

### Recommandations impératives

1. Charger le modèle en **lazy** (premier usage du champ NLP, pas au démarrage)
2. Exécuter l'inférence dans un **Isolate Dart** séparé
3. Prévoir un fallback gracieux si RAM insuffisante (try/catch + message user)

**Score fluidité : 7/10**

---

## D. Faisabilité technique concrète

| Étape | Difficulté | Risque | Détail |
|---|---|---|---|
| Intégrer `tflite_flutter` | Moyenne | **Danube 3 GGUF ≠ TFLite format** | GGUF quantifié n'est PAS nativement compatible TFLite. Il faudrait convertir (perte qualité) ou utiliser `llama.cpp` via FFI. |
| Téléchargement on-demand | Facile | Aucun | Dio + `path_provider` + progress callback. Déjà maîtrisé dans le projet. |
| Parser NLP → EventModel | Élevée | Parsing fragile | Le LLM sort du texte brut. Il faut un prompt structuré + parsing robuste de la sortie (dates, heures, lieux, tags). Beaucoup de regex/heuristiques. |
| Tests & fiabilité | Élevée | Non-déterministe | L'IA locale n'est pas déterministe. Résultats variables selon la formulation. Tests unitaires difficiles. |

**Score faisabilité technique : 5/10**

---

## E. Alternative stratégique recommandée

Avant de déployer un LLM de 500 Mo, évaluer une approche **hybride légère** :

### Option 1 : Parser Regex/Heuristiques (recommandé pour V3.0)

- **Poids ajouté** : 0 Mo
- **Latence** : instantanée
- **Souveraineté** : parfaite (zéro réseau, zéro modèle externe)
- **Couverture** : ~80% des cas "power user"
- **Exemple** : `"Dîner avec @Marc vendredi 20h au Resto #Perso"` →
  - `@Marc` → participant
  - `vendredi 20h` → date relative parsée
  - `au Resto` → location
  - `#Perso` → tag catégorie
  - Le reste → titre
- **Effort** : ~200 lignes de Dart, testable unitairement

### Option 2 : LLM local (V3.1 si regex insuffisant)

- Upgrade optionnel téléchargeable
- Uniquement si le parser regex ne couvre pas assez de cas
- À implémenter avec `llama.cpp` FFI plutôt que TFLite (compatibilité GGUF native)

---

## F. Scores finaux Phase 3

| Critère | Note | Commentaire |
|---|---|---|
| Sécurité / Souveraineté | **9/10** | Excellent — Zero Trust by design |
| Poids application | **6/10** | À optimiser (APK actuel trop gros) |
| Fluidité | **7/10** | Bon sur mid-range+, risque OOM sur 4 Go |
| Faisabilité technique | **5/10** | GGUF ≠ TFLite, parsing fragile |
| **Moyenne** | **6.5/10** | **Faisable mais coûteux en effort** |

### Recommandation finale

> **Phase 2 → Parser Regex (V3.0) → LLM local optionnel (V3.1)**
>
> 1. Terminer les tests terrain de la Phase 2 sur Android
> 2. Implémenter le parser regex/heuristiques (~200 lignes, 0 Mo, instantané)
> 3. Tester en usage réel pendant 2-4 semaines
> 4. Si insuffisant → implémenter le LLM local via `llama.cpp` FFI

---

*Document généré automatiquement par Opus 4.6 — basé sur l'analyse complète du code source (18 152 lignes Dart, 107 fichiers modifiés).*
