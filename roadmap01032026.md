# ROADMAP monAgenda — 01/03/2026
## Feuille de route — Audit STRICT contre le code réel (HEAD = `9c4743e`)

> **Méthodologie :** Étape par étape. Chaque tâche = test + `flutter analyze` + commit atomique.
> **Base de référence :** `manifest032026.md`, `DESIGN_SYSTEM.md`, audit adversarial complet du 02/03/2026.
> **Codebase :** 62 fichiers Dart, ~23 000 lignes, HEAD = `9c4743e`.
> **Dernière vérification :** 02/03/2026 — chaque [x] vérifié fichier/ligne, chaque [ ] confirmé absent.

---

## PHASE 0 — SÉCURITÉ CRITIQUE (Priorité absolue) — 7/7 ✅

> ⚠️ Aucune feature ne doit être ajoutée tant que la sécurité n'est pas solidifiée.

- [x] **0.1** — Certificate pinning via TOFU dynamique (`CertPinManager` dans `cert_pin_manager.dart`). Les hash sont calculés au premier contact et stockés en BDD (table `cert_pins`). CAs de confiance listées en dur pour auto-rotation. ⚠️ Pas de SHA-256 hardcodés statiques — c'est du TOFU, pas du pinning classique. ✅
- [x] **0.2** — `verifyCertificates()` présent dans `infomaniak_service.dart` L37–39 et `notion_service.dart` L28–30 via `CertPinManager.instance`. TOFU + auto-rotation par CA connue. ✅
- [x] **0.3** — SQLCipher actif : `sqflite_sqlcipher: ^3.1.0+1` dans pubspec.yaml. Desktop : PRAGMA key + migration auto (`_migrateToEncryptedIfNeeded`). Mobile : `openDatabase(path, password: dbKey)`. Clé dans `FlutterSecureStorage`. ✅
- [x] **0.4** — PBKDF2-HMAC-SHA256 (100k iter, salt 32B) dans `backup_service.dart` L199–211. Magic header `MAP2`. Rétrocompat lecture via `_legacyKey`. ✅
- [x] **0.5** — `toExportString()`/`fromExportString()` deprecated supprimés. Seuls `toEncryptedExportString()` et `fromEncryptedExportString()` existent dans `settings_provider.dart`. ✅
- [x] **0.6** — `DatabaseHelper.close()` : `_database = null` après `db.close()` confirmé L1229–1233. ✅
- [x] **0.7** — `FlutterError.onError` (main.dart L41–47) + `PlatformDispatcher.instance.onError` (L50–57) → `AppLogger`. ✅

---

## PHASE 1 — BUGS BLOQUANTS & CORRECTIONS CODE

> Bugs vérifiés contre le code réel. Chacun a un impact fonctionnel direct.

### 1A — Bugs critiques — 8/8 ✅

- [x] **1.1** — Bouton recherche : `agenda_screen.dart` L224–236 → `Navigator.push → SearchScreen()`. ✅
- [x] **1.2** — `SearchScreen` importé dans `agenda_screen.dart` L20 et `app.dart` L16, accessible via bouton + Ctrl+F. ✅
- [x] **1.3** — `copyWith` dans `NotionTaskModel` (project_view_screen.dart L31–45) : 5/5 champs, retourne nouvelle instance. ✅
- [x] **1.4** — `EventModel.props` Equatable complet (**26 champs**, pas 27 — event_model.dart L254–281). Tous les champs de la classe sont couverts. ✅
- [x] **1.5** — Source magic entry dynamique : `magic_entry_service.dart` L63–72 (Infomaniak > Notion > fallback). ✅
- [x] **1.6** — `WidgetService.updateWidget()` appelé après create (L119), update (L145), delete (L158) dans `events_provider.dart`. ✅
- [x] **1.7** — `deleteEvent` utilise `DatabaseHelper.instance` directement (SQL, pas Riverpod state). ✅
- [x] **1.8** — `db_migrations.dart` : `kCurrentDbVersion = 14` + `kMigrations` V7→V14. ✅

### 1B — Bugs techniques — 10/10 ✅

- [x] **1.9** — Timer leaks : 3 timers (`_autoClearTimer`, `debounceTimer`, `Timer.periodic`) tous avec `ref.onDispose(() => ...cancel())` dans `sync_provider.dart`. ✅
- [x] **1.10** — `hiddenTagsProvider` : 0 occurrences dans `lib/` — rien à corriger. ✅
- [x] **1.11** — N+1 queries : `_mapToEventsWithTags` fait un batch unique `SELECT ... WHERE event_id IN (?,?...)` dans `database_helper.dart` L667–695. ✅
- [x] **1.12** — Recherche participants sécurisée : `whereArgs` paramétré dans `database_helper.dart` L645–646. ✅
- [x] **1.13** — Déduplication `sync_queue` : DELETE UPDATE pending avant INSERT dans `database_helper.dart` L905–921. ⚠️ Ne couvre que `action == 'update'`, pas create/delete (acceptable). ✅
- [x] **1.14** — FTS5 : migration V8 + fallback LIKE via `_hasFtsTable(db)` dans `database_helper.dart` L618–633. ✅
- [x] **1.15** — **Bannière serveur saturé** : `_isServerError()` détecte 429/500/503/timeout dans `sync_engine.dart` L600–613. `serverSyncErrorProvider` dans `sync_provider.dart` L77–78. Bannière UI orange + bouton "Réessayer" dans `app.dart` L1190–1243. ✅
- [x] **1.16** — **Conflits ETag (HTTP 412)** : HEAD pour ETag frais avant PUT (`infomaniak_service.dart` L178–190). Gestion 412 → last-remote-wins L206–240. `etagConflictProvider` dans `sync_provider.dart` L114. UI listener dans `app.dart` L595. ✅
- [x] **1.17** — Audit `print()` : 1 seul dans `logger_service.dart` L19, dans un `assert()` debug-only. ✅
- [x] **1.18** — Gestion logs BDD : cap 1000 (`database_helper.dart` L1002), auto-trim toutes les 50 insertions (L1028–1031), purge >7j (L1110), cleanup au démarrage L1141–1144. ✅

---

## PHASE 2 — UX / ERGONOMIE (Points audit + tes demandes)

### 2A — Navigation et accès rapides

- [x] **2.1** — Boutons **"Ouvrir Notion"** / **"Ouvrir Infomaniak"** dans NavigationRail Desktop (`app.dart` L782–828) + gestion overflow > 3 BDD Notion. ✅
- [x] **2.2** — Ctrl+F ouvre la recherche : `app.dart` L1028–1033 → `Navigator.push → SearchScreen()` via `CallbackShortcuts`. ✅
- [ ] **2.3** — Raccourcis clavier Desktop : **4/7 implémentés** (Ctrl+N, Ctrl+K, Ctrl+F, Escape dans `app.dart` L1011–1041). **MANQUANTS : Ctrl+S, Delete, Espace, Tab** + tooltips sur les boutons
- [x] **2.4** — Vrais logos PNG Infomaniak/Notion : `source_logos.dart` utilisé dans agenda_screen, calendar_screen, event_block, unified_event_card, event_detail_screen. ✅
- [x] **2.4b** — Bug affichage compact corrigé — 18 overflow issues résolues. ✅

### 2B — Fonctionnalités demandées

- [ ] **2.5** — **Import ICS direct** : aucun import fichier local `.ics` (seulement abonnement URL). Pas de `file_picker` dans le flux ICS.
- [x] **2.6** — **Panneau Todo (tâches sans date)** : `project_view_screen.dart` — `_buildTaskPanel()` L283, filtre Notion `date: {is_empty: true}` L57, split-panel calendrier + tâches L218. ✅
- [x] **2.7** — **Redimensionnement par poignée** : `calendar_screen.dart` → `allowAppointmentResize: true` L636, `_onAppointmentResizeEnd()` L1619 avec snap 15min + durée min 15min + protection .ics readonly. ✅
- [x] **2.8** — **Logo dock Ubuntu** : `fr.monagenda.app.desktop` avec `Icon=monagenda`, icônes PNG dans `linux/icons/hicolor/` (16→512px). ✅

### 2D — Features V3 (Desktop)

- [x] **2.15** — **Meeting Note Notion** : bouton "Ouvrir/Créer le compte-rendu" dans `event_detail_screen.dart` L155–157/L796–851, service `notion_meeting_service.dart`. ✅
- [x] **2.16** — **Smart Attachments** : `_buildAttachmentsSection()` dans `event_detail_screen.dart` L641–762, colonne `smart_attachments TEXT` en DB, migration V7. ✅
- [x] **2.17** — **Import QR d'événements** : `import_qr_screen.dart` (316 lignes) — scan mobile `mobile_scanner` + import image desktop `file_picker` + merge intelligent `ImportNotifier.mergeImport()`. ✅
- [ ] **2.18** — **Logos SVG monAgenda** : aucun SVG dans `assets/logos/`, pas de `flutter_svg`. Tout est en PNG (16→512px).

### 2C — États vides et feedback visuel

- [ ] **2.9** — Empty states : **PARTIEL** — agenda (`_buildEmptyState()` L879) et search (L296–306) ont des empty states. **MANQUANT** : vue calendrier SfCalendar, vue événements détaillée.
- [x] **2.10** — Mode AMOLED : `app.dart` L322–344 `_buildDarkTheme(bool isAmoled)` + `Color(0xFF000000)` + toggle dans `appearance_settings_screen.dart` L163–175. ✅
- [ ] **2.11** — Bouton "Réinitialiser l'app" : **ABSENT**. Aucun reset/dropAndRecreate dans les paramètres.
- [ ] **2.12** — Bouton "Tester la connexion kDrive" : **ABSENT**. `testKDriveConnection` existe dans `backup_service.dart` L316–356 mais **aucun bouton UI** ne l'expose dans settings.
- [ ] **2.13** — Notification tap → événement : payload `'event:${event.id}'` envoyé (`notification_service.dart` L85) mais `_onNotificationTapped()` L62–64 est un **stub vide** (commentaire "Sera géré par le router").
- [x] **2.14** — **Paramètres 5 sections** : `settings_screen.dart` L44–50 — 5 sections exactes ('Comptes', 'Notifications', 'Apparence', 'IA & Modèle', 'Avancé'). NavigationRail Desktop L81 + ExpansionTile Mobile L109–125. ✅

---

## PHASE 3 — STOCKAGE SOUVERAIN (Zéro fichier local)

> **Principe : on ne stocke aucun fichier permanent dans l'application. Les fichiers sont stockés soit sur Infomaniak soit dans Notion.**
> **État actuel :** Le backup est 100% local (fichier AES chiffré retourné à l'UI). `testKDriveConnection()` existe mais aucun upload WebDAV automatique.

- [ ] **3.1** — Audit de tous les endroits où l'app écrit des fichiers locaux (backup, export ICS, export CSV, export PPT, modèle IA) → documenter chaque cas
- [ ] **3.2** — Backup : remplacer l'écriture locale + copier-coller par un upload direct WebDAV vers kDrive Infomaniak
- [ ] **3.3** — Export ICS/CSV : proposer l'option "Envoyer vers kDrive" en plus de la sauvegarde locale
- [ ] **3.4** — **ModelDownloadService complet** : télécharger le .gguf depuis kDrive/HuggingFace → stocker dans `getApplicationDocumentsDirectory()` + vérification SHA-256 post-téléchargement + progress stream UI + reprise partielle via HTTP Range + `ModelIntegrityException` si hash mismatch
- [ ] **3.5** — S'assurer que `getDownloadsDirectory()` n'est utilisé que comme fallback temporaire, jamais comme stockage permanent

---

## PHASE 4 — WIDGET ANDROID FONCTIONNEL — 4/5 ✅

> Le plugin `home_widget: ^0.7.0` est dans `pubspec.yaml`. Le `CalendarWidgetProvider.kt` et `WidgetService.dart` sont connectés.

- [x] **4.1** — `WidgetService.updateWidget()` après create (L127), update (L148), delete (L160) dans `events_provider.dart`. ✅
- [x] **4.2** — `WidgetService.updateWidget()` après sync dans `sync_engine.dart` L257–259. ✅
- [x] **4.3** — Layout XML : `calendar_widget.xml` (LinearLayout 7 paires title/time) + `calendar_widget_info.xml`. ✅
- [x] **4.4** — Receiver : `fr.monagenda.app.CalendarWidgetProvider` dans `AndroidManifest.xml` L81–88 avec `APPWIDGET_UPDATE`. ✅
- [ ] **4.5** — Tester le widget sur un émulateur Android (les 7 prochains jours doivent s'afficher)

---

## PHASE 5 — INTELLIGENCE ARTIFICIELLE (Danube 3 → Assistant)

> Actuellement Danube 3 500M fait du parsing NLP (texte → EventModel). L'objectif est de le rendre plus "assistant".

- [ ] **5.1** — **Améliorer le Regex-first** : durées "2h" ✅, créneaux "matin/après-midi/soir" ✅, lieux "à/au/chez/@" ✅ sont OK. **MANQUANT : mois en lettres** ("15 mars", "3 avril") — aucune regex ne matche les dates textuelles, seules les dates numériques (`15/03`) sont couvertes.
- [x] **5.1b** — **GBNF grammar enforcement** : `gbnfGrammar` constante dans `llama_service.dart` L35–40, injectée via `SamplerParams.grammarStr` L108. ✅
- [x] **5.1c** — **Validateur post-IA** : `buildFromIaJson` vérifie title vide (L401–415), `endDateTime.isBefore(startDateTime)` (L1104–1112), heures 0-23. ✅
- [x] **5.2** — **Feedback loop IA** : table `magic_feedback` (migration db_migrations.dart L28), CRUD via `magic_feedback_repository.dart`. ✅
- [x] **5.3** — **Magic habits** : table `magic_habits` (db_migrations.dart L36–45), `magic_habits_repository.dart`, apprentissage via `learnFromCorrection()` L183–215. ✅
- [ ] **5.4** — **Suggestions contextuelles** : **ABSENT**. Aucun overlay d'auto-complétions dans `magic_entry_screen.dart`.
- [ ] **5.5** — **Résumé matinal intelligent** : `scheduleDailySummary()` existe (`notification_service.dart` L120–167) mais le body est **statique** : "Appuyez pour voir vos événements du jour". Pas d'appel IA.
- [ ] **5.6** — **Mode conversation libre** : **ABSENT**. Aucune trace dans le codebase.
- [x] **5.7** — **Déchargement mémoire** : `dispose()` dans `llama_service.dart` L223–234 libère le modèle (`_parent!.dispose()`, `_isModelLoaded = false`). Appelé aussi avant rechargement L121. ✅

### Faisabilité du mode assistant (5.6)

> **Est-ce possible avec Danube 3 500M ?**
> 
> ✅ **Oui, partiellement.** Danube 3 est un SLM (Small Language Model) fine-tuné sur l'extraction d'information structurée. Il peut :
> - Parser des questions simples en intentions (QUERY_NEXT, QUERY_COUNT, QUERY_WHEN)
> - Extraire des entités (personne, date, type d'event) depuis une question naturelle
> - Produire un JSON structuré `{"intent": "QUERY_NEXT", "person": "Marc", "type": "réunion"}`
> 
> ❌ **Ce qu'il ne peut PAS faire :**
> - Générer des phrases naturelles fluides (ce n'est pas GPT-4)
> - Raisonner sur des données complexes (agrégations, comparaisons temporelles)
> - Maintenir un contexte conversationnel multi-tour
> 
> **Stratégie proposée :** Danube génère un JSON d'intention → le code Dart execute la query SQLite → le résultat est formaté en template Dart ("Votre prochain rdv avec Marc est le 5 mars à 14h"). Le LLM n'écrit PAS la réponse, il extrait l'intention.

---

## PHASE 6 — REFONTE GRAPHIQUE & DESIGN SYSTEM

> Harmonisation complète avec `DESIGN_SYSTEM.md`. Material 3 déjà actif.

- [x] **6.1** — Palette **100 couleurs Stabilo Boss × Paper Mate Flair** (10 familles × 10 nuances) dans `app_colors.dart` L3–7. ✅
- [x] **6.2** — Cards harmonisées : elevation 0, border subtile, borderRadius 3px dans `unified_event_card.dart`. ✅
- [x] **6.3** — TextFields harmonisés : filled, fond Notion, borderRadius 8, border 1px. ✅
- [x] **6.4** — SnackBars harmonisés : `SnackBarBehavior.floating` dans `app.dart` L203. ✅
- [x] **6.5** — Police Inter : `GoogleFonts.interTextTheme()` dans `app.dart` L110. ✅
- [ ] **6.6** — Micro-animations : **PARTIEL** — FadeTransition OK (`unified_event_card.dart` L115), AnimatedSwitcher OK (`agenda_screen.dart` L126). **MANQUANT : SlideTransition** sur BottomSheet.
- [ ] **6.7** — `disableAnimations` : **PARTIEL** — `WidgetsBinding.instance.disableAnimations` dans `unified_event_card.dart` L48 uniquement. Non appliqué dans `agenda_screen.dart` AnimatedSwitcher.

---

## PHASE 7 — ONBOARDING & FINITIONS

- [x] **7.1** — Écran `SetupScreen` : `lib/features/setup/setup_screen.dart` existe avec PageView onboarding. ✅
- [x] **7.2** — Guard de routage : `app.dart` L68–70 — `home: settings.isInfomaniakConfigured ? AppShell() : SetupScreen()`. ⚠️ Ne couvre que Infomaniak — si seul Notion est configuré, redirigé vers Setup. ✅
- [x] **7.3** — `ForceOfflineNotifier` migré : `extends Notifier<bool>` dans `sync_provider.dart` L328–330 (Riverpod 3 ready). ✅
- [x] **7.4** — `notionApiVersion = '2022-06-28'` dans `app_constants.dart` L10. C'est la dernière version stable de l'API Notion. ✅
- [x] **7.5** — `flex_color_scheme` : **absent** de `pubspec.yaml` et absent de tout `import` dans `lib/`. Déjà supprimé. ✅
- [x] **7.6** — `RadioListTile` migré vers `RadioGroup` dans magic_entry_screen, appearance_settings_screen, settings_screen, event_form_screen. ✅ (audit 02/03/2026)
- [ ] **7.7** — `IntrinsicHeight` dans `search_screen.dart` L338 : **encore présent**, à supprimer pour la performance.
- [ ] **7.8** — Optimisation APK : `minifyEnabled true` et `shrinkResources true` déjà dans `build.gradle` L58–59. **MANQUANT : `--split-per-abi`**, audit `const` widgets, audit `dispose()` global, vérification tree-shaking assets.

---

## PHASE 8 — DOCUMENTATION & VERSIONING

- [ ] **8.1** — Mettre à jour `README.md` avec l'architecture V3 réelle
- [ ] **8.2** — Mettre à jour `DESIGN_SYSTEM.md` avec les tokens réellement appliqués
- [ ] **8.3** — Corriger `V3GK.md` : section QR (pipeline AES, pas gzip) + section CalDAV (nomenclature `InfomaniakService` pas `CalDAVClient`)
- [ ] **8.4** — Tag Git `v3.0.0` quand toutes les phases sont complètes

---

## RÉCAPITULATIF — État réel vérifié le 02/03/2026

| Phase | Fait | Reste | Priorité | Description |
|-------|------|-------|----------|-------------|
| **0** | 7/7 | 0 | 🔴 CRITIQUE | Sécurité — **TERMINÉE** |
| **1** | 18/18 | 0 | 🔴 HAUTE | Bugs bloquants — **TERMINÉE** |
| **2** | 12/18 | 6 | 🟠 HAUTE | UX, ergonomie, features V3 |
| **3** | 0/5 | 5 | 🟠 MOYENNE | Stockage souverain kDrive |
| **4** | 4/5 | 1 | 🟡 MOYENNE | Widget Android |
| **5** | 5/9 | 4 | 🟡 MOYENNE | IA (suggestions, résumé, conversation) |
| **6** | 5/7 | 2 | 🔵 NORMALE | Design System |
| **7** | 6/8 | 2 | 🔵 NORMALE | Finitions, optim |
| **8** | 0/4 | 4 | ⚪ BASSE | Documentation |
| **TOTAL** | **57/81** | **24** | | **70% terminé** |

---

## TÂCHES RESTANTES ORDONNÉES PAR PRIORITÉ

### 🟠 Priorité haute (Phase 2 restante)
- [ ] **2.3** — Raccourcis clavier manquants : Ctrl+S, Delete, Espace, Tab + tooltips
- [ ] **2.5** — Import fichier `.ics` direct (sélecteur de fichier)
- [ ] **2.9** — Empty states manquants (vue calendrier, détail)
- [ ] **2.11** — Bouton "Réinitialiser l'app" + double confirmation
- [ ] **2.12** — Bouton UI "Tester connexion kDrive" (le service existe déjà)
- [ ] **2.13** — Navigation notification tap → événement (stub vide à compléter)

### 🟡 Priorité moyenne
- [ ] **2.18** — Logos SVG monAgenda (SplashScreen, AboutScreen)
- [ ] **3.1–3.5** — Stockage souverain kDrive (5 tâches)
- [ ] **4.5** — Test widget Android sur émulateur
- [ ] **5.1** — Regex mois en lettres ("15 mars", "3 avril")
- [ ] **5.4** — Suggestions auto-complétion barre magique
- [ ] **5.5** — Résumé matinal intelligent (IA)
- [ ] **5.6** — Mode conversation libre

### 🔵 Priorité normale
- [ ] **6.6** — SlideTransition sur BottomSheet (FadeTransition et AnimatedSwitcher déjà OK)
- [ ] **6.7** — `disableAnimations` sur tous les widgets animés
- [ ] **7.7** — Supprimer `IntrinsicHeight` dans search_screen.dart
- [ ] **7.8** — `--split-per-abi` + audit const/dispose/tree-shaking

### ⚪ Basse
- [ ] **8.1–8.4** — Documentation (README, DESIGN_SYSTEM, V3GK, tag v3.0.0)

---

*Rédigé le 01/03/2026. Mis à jour le 02/03/2026. Vérification stricte fichier/ligne le 02/03/2026 (session Isabelle). HEAD = `9c4743e`.*
